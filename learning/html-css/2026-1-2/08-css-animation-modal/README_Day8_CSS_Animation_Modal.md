# Day 8 — CSS Animation Theory & Bezier Modal (실무형)

**Date:** Fri, Jan 2, 2026  
**Topic:** CSS Transition / Easing (cubic-bezier) / Modal UX (A11y + 성능)  
**Goal:** 베지에 곡선 원리를 이해하고 **커스텀 이징 모달**을 구현한다. (실무형 UX + 성능 + 접근성)

> **정직한 기록**  
> 오늘 구현에서 JavaScript는 “내가 완전히 설계/작성”한 것이 아니라, **템플릿/가이드를 따라치며 구조를 이해한 수준**이다.  
> 다만 **JS가 어떤 역할로 CSS 애니메이션(transition)과 상호작용하는지**(상태 토글, 데이터 주입, 이벤트 흐름)는 명확히 이해하고 넘어간다.

---

## ✅ 오늘의 진짜 핵심

애니메이션은 “움직이는 UI”가 아니라 **상태 변화를 납득시키는 설계**다.

- **State 변화(열림/닫힘)** → **Motion(이동/페이드)** → **Easing(감정 곡선)**
- 모달 애니메이션의 실무 표준은 대부분 아래 2개 속성으로 끝난다:
   - `opacity` (존재감)
   - `transform` (위치감)

> `transition: all`은 편해 보이지만 **불필요한 속성까지 애니메이션**되어 성능/버그가 늘어난다.  
> 실무에선 **opacity + transform만** 설계하는 편이 안정적이다.

---

## 🧠 완벽 이론 정리 (실무 기준)

### 1) Transition vs Animation — “언제 뭘 쓰는가”가 핵심

#### Transition

- **두 상태(A → B) 사이** 변화에 최적
- Trigger: `:hover`, `:focus`, `classList.toggle()` 등
- 모달/드롭다운/토글 UI 대부분은 Transition으로 충분

✅ 장점

- 상태가 2개(닫힘/열림)일 때 구현이 단순
- 유지보수 쉬움 / 버그 적음

#### Animation (@keyframes)

- **여러 단계(0%~100%)** 시나리오에 최적
- Trigger: 로딩, 강조, 반복(infinite), 튕김/진동 같은 연출

✅ 장점

- 중간 단계 연출 가능
- 반복/지연/멀티 스텝에 강함

---

### 2) Easing(이징) = “감정 곡선”

같은 시간/같은 거리라도 **속도 변화**가 다르면 느낌이 완전히 달라진다.

- `linear`: 기계적(감정 없음)
- `ease`: 무난한 기본값
- `cubic-bezier(...)`: “내가 설계한 감정”

> 좋은 이징은 “눈에 띄는 연출”이 아니라  
> **사용자가 자연스럽게 받아들이는 속도감**이다.

---

### 3) cubic-bezier(x1, y1, x2, y2) — 이해 포인트만 딱

- 곡선은 “**시간 대비 진행률**”을 만든다.
- x축: 시간(0→1), y축: 진행률(0→1)
- **x 값은 보통 0~1**에 둔다 (시간이 뒤집히지 않게)
- y 값은 -값 / 1 초과도 가능(오버슈트 느낌) — 다만 과하면 UI가 “과장”되어 보일 수 있음

자주 쓰는 실무 이징 예시

```css
/* 부드러운 기본(추천) */
cubic-bezier(0.2, 0.8, 0.2, 1);

/* 빠르게 시작 → 천천히 멈춤 (UI에 자주 씀) */
cubic-bezier(0.16, 1, 0.3, 1);

/* 살짝 튕김(오버슈트 느낌) */
cubic-bezier(0.34, 1.56, 0.64, 1);
```

#### Q) cubic-bezier 말고 다른 버전도 있나?

있다. 실무에선 아래도 빈번히 사용한다.

- **키워드 이징**
   - `linear`, `ease`, `ease-in`, `ease-out`, `ease-in-out`
- **스텝 이징(툭툭 끊기는 연출)**
   - `steps(n, start|end)`
   ```css
   transition-timing-function: steps(5, end);
   ```

#### Q) cubic-bezier가 트렌드/실무에서 제일 많이 쓰나?

- “가장 많이 쓰이는 건” `ease` 또는 UI 프레임워크 기본 이징이지만,
- **제품/브랜드 톤을 정확히 만들고 싶을 때** `cubic-bezier`가 실무에서 매우 많이 쓰인다.  
  즉, **‘트렌드’라서가 아니라 ‘제어 가능’해서** 사용된다.

---

### 4) 성능 실무 규칙 (버벅임 줄이는 핵심)

✅ GPU 친화(가능하면 이걸로만)

- `transform`
- `opacity`

❌ 비용 큼(레이아웃/페인트 유발 가능)

- `width/height`, `top/left`, `margin` (Reflow)
- `box-shadow` (무거울 수 있음)

---

### 5) 접근성(A11y) 실무 포인트 (모달은 여기서 점수 갈림)

- 모달 열리면 **포커스가 모달로 이동**
- `Esc`로 닫기(표준 UX)
- Dim 클릭 닫기 가능하되, **모달 내부 클릭 전파 차단**
- 모션 선호도 대응: `prefers-reduced-motion`

---

## 🧩 JS는 CSS 애니메이션과 어떻게 상호작용하나?

오늘의 JS는 “애니메이션을 만든다”가 아니라, **상태를 바꾸는 스위치** 역할이다.

- `document.body.classList.add('is-open')`
   - CSS에서 `.is-open .modal-panel` 규칙이 적용되면서 **transition이 실행**
- `aria-hidden` 토글
   - 접근성 상태(모달이 읽히는지/숨겨지는지) 제어
- `focus()` 이동
   - 키보드 사용자가 “배경”이 아닌 “모달”을 조작하도록 유도

즉, **JS는 상태를 바꾸고, CSS는 그 상태 변화의 모션을 설계**한다.

---

## 🏗️ 구현 요구사항 (완성 기준)

- [x] Open 버튼으로 모달 열림 (`opacity + transform`)
- [x] Close 버튼으로 닫힘
- [x] Dim(Backdrop) 클릭으로 닫힘
- [x] `cubic-bezier`로 열림/닫힘 톤 통일
- [x] `transition: all` 사용 금지 (`opacity/transform`만)
- [x] `Esc` 닫기
- [x] `prefers-reduced-motion` 대응

---

## 🧱 구조 설계 원칙 (실무형)

- Dim(배경)과 Modal(콘텐츠)을 **분리**
- 상태는 `.is-open` 한 개로 통일
- 열림/닫힘 애니메이션:
   - Dim: `opacity`
   - Modal: `transform + opacity`
- 숨김 처리:
   - `opacity` + `visibility` 조합(애니메이션 끊김 방지)

---

## 📌 오늘 질문했던 내용 — 핵심 보강

### 1) 이미지: background vs img 태그, 실무는?

- **정보(콘텐츠) 이미지**: `<img>`가 정석(alt, 접근성, SEO)
- **장식(데코) 이미지**: `background-image`가 적합(의미 없음)

### 2) img에 직접 스타일? CSS 파일?

- 기본 원칙: **스타일은 CSS로**, HTML에는 의미/구조
- 예외: 실험/데모, 이메일 HTML, JS로 인라인 수치 조작 등

### 3) `aspect-ratio: 4 / 3` 의미

- “4분의 3만 쓴다”가 아니라  
  **가로:세로 비율을 4:3으로 유지**한다는 뜻
- 카드/썸네일에서 레이아웃 흔들림(CLS) 방지에 효과적

### 4) 미디어쿼리 문법

```css
@media (max-width: 720px) { ... }   /* 720px 이하 */
@media (min-width: 720px) { ... }   /* 720px 이상 */
```

### 5) 모바일에서 카드가 중앙 정렬이 안 됐던 이유

- `flex-direction: column`이면 **가로 중앙 정렬은** `align-items: center`가 담당

```css
@media (max-width: 720px) {
   .products__list {
      flex-direction: column;
      align-items: center;
   }
   .product__item {
      width: 100%;
      max-width: 400px;
   }
}
```

---

## 💻 구현 코드(요약)

### 핵심 CSS (모달)

```css
.modal-backdrop {
   opacity: 0;
   visibility: hidden;
   transition:
      opacity var(--dur) var(--ease-modal),
      visibility 0s linear var(--dur);
}
.modal-panel {
   opacity: 0;
   visibility: hidden;
   transform: translate(-50%, calc(-50% + 16px));
   transition:
      opacity var(--dur) var(--ease-modal),
      transform var(--dur) var(--ease-modal),
      visibility 0s linear var(--dur);
}

.is-open .modal-backdrop {
   opacity: 1;
   visibility: visible;
   transition: opacity var(--dur) var(--ease-modal);
}
.is-open .modal-panel {
   opacity: 1;
   visibility: visible;
   transform: translate(-50%, -50%);
   transition:
      opacity var(--dur) var(--ease-modal),
      transform var(--dur) var(--ease-modal);
}
```

### 핵심 JS (모달 “상태 토글”)

```js
function openModal() {
   lastFocus = document.activeElement;
   document.body.classList.add('is-open');
   $root.setAttribute('aria-hidden', 'false');
   $close.focus();
}

function closeModal() {
   document.body.classList.remove('is-open');
   $root.setAttribute('aria-hidden', 'true');
   if (lastFocus && typeof lastFocus.focus === 'function') lastFocus.focus();
}
```

---

## ✅ 오늘 얻은 것(실무 관점)

- 애니메이션은 “연출”이 아니라 **상태 설계**
- `opacity + transform` 중심으로 성능과 UX를 함께 잡는 법
- `cubic-bezier`로 UI 톤(감정 곡선)을 제어하는 방법
- 모달에서 기본으로 챙겨야 하는 접근성: 포커스/ESC/reduced-motion
- 반응형에서 `flex-direction: column` 시 정렬 축 개념(align-items)

---

## 참고 키워드(추가 학습)

- `cubic-bezier easing`
- `prefers-reduced-motion`
- `opacity transform transition performance`
- `modal accessibility focus management`
